version: "3.8"

services:
  app:
    build: .
    container_name: osums_app
    network_mode: "host"
    volumes:
      - .:/var/www/html
    ports:
      - "8000:8000"
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_KEY: "base64:8jMJVmFmwHQnnj63uBR6xfa/qk251M7NIX36V/Ry0+k="
